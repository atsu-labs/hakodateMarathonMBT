{
  "private": true,
  "workspaces": [
    "backend",
    "frontend-admin",
    "frontend-mobile"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:frontend": "npm run build --workspace=frontend-admin && npm run build --workspace=frontend-mobile"
  }
}